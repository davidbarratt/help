language: bash
services:
  - docker
before_script:
  - 'docker-compose up -d api'
  - 'docker-compose run composer install'
script:
  - 'docker-compose exec api ./vendor/bin/phpcs --report=summary .'
