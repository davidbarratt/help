language: bash
services:
  - docker
before_script:
  - 'docker-compose up -d api'
  - 'docker-compose run composer install'
script:
  - 'docker-compose run composer exec -- "phpcs -p --report=summary ."'
  - 'docker-compose run composer exec -- "phpunit"'
